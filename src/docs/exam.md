

## Билет №1

1. Сформулируйте **пять обязательных характеристик облачных вычислений** по NIST. Почему отсутствие любой из них (например, Measured Service или On-Demand Self-Service) выводит инфраструктуру из категории «облако», даже если она физически развёрнута в дата-центре? Приведите конкретный пример «псевдо-облака», нарушающего одну из характеристик.
2. Объясните разницу между **гипервизорами 1-го и 2-го типов**. Какие компоненты гипервизора (VMM, ядро) выполняют какую функцию? Почему steal-time может оказаться высоким даже на «свободном» по другим метрикам хосте?
3. На примере **vendor-lock** объясните, почему это «риск, а не зло». Какие пять конкретных групп проблем порождает vendor-lock и как стратегия миграции (например, Refactoring vs Lift-and-Shift) влияет на глубину блокировки потребителя у конкретного вендора?

---

## Билет №2

1. Поясните разницу между **Public, Private, Hybrid и Community Cloud**. По каким критериям организация выбирает гибридную модель, а не уход полностью в публичное облако? Какие требования регуляторов и понятие «технологический суверенитет» влияют на этот выбор в РФ?
2. Что такое **subject КИИ (критической информационной инфраструктуры)** и какие три ключевые задачи стоят перед такими субъектами? Как требования к КИИ ограничивают свободу выбора облачных провайдеров и open-source ПО?
3. Расскажите про **эталонную модель облачной инфраструктуры**: Virtual / Control / Orchestration. Какие задачи решает каждый уровень и почему попытка «срезать» уровень Orchestration в зрелой системе ведёт к катастрофическому росту операционных затрат?

---

## Билет №3

1. Перечислите и подробно раскройте **7 принципов контейнеризации** (SCP, HOP, LCP, IIP, PDP, Self-containment, RCP). Какие из них наиболее часто нарушаются на практике и к каким операционным последствиям это приводит?
2. Объясните внутреннее устройство Docker: **namespaces, cgroups, Union File System**. Почему именно эта тройка примитивов даёт изоляцию без полноценной виртуализации ядра? Какие классы атак она при этом *не* предотвращает?
3. Что такое **multi-stage build** и зачем он нужен? Почему «толстый» образ с `curl`, компиляторами и debug-инструментами — это не только проблема дискового места, но и проблема **информационной безопасности** (увеличение поверхности атаки)?

---

## Билет №4

1. Сформулируйте принцип **Image Immutability (IIP)**. Как он сочетается с необходимостью передавать в контейнер конфиденциальные данные (пароли, ключи API)? Опишите цепочку: переменные окружения → секрет-менеджер (Vault) → ротация — и какие антипаттерны она устраняет.
2. Объясните, что такое **healthcheck** в Docker/Kubernetes и в чём принципиальная разница между **liveness, readiness и startup probes** в Kubernetes. Какие антипаттерны связаны с проверкой внешних зависимостей в liveness probe и почему это особенно опасно?
3. Опишите устройство **Pod** в Kubernetes и роль **Deployment**. Почему Kubernetes не запускает контейнеры напрямую, а оборачивает их в Pod? Какие гарантии (общая сеть, общие volumes, lifecycle) обеспечивает Pod, и какие задачи нельзя решать парой контейнеров «в одном поде»?

---

## Билет №5

1. Что такое **CI/CD** и из каких уровней (Pipeline → Stage → Job → Step) она состоит? Что выполняется на **runner** и почему запуск всех job-ов на shared-runner-е в продакшен-окружении — антипаттерн с точки зрения безопасности?
2. Сформулируйте все **12 факторов 12-Factor App**. Для каждого фактора приведите по одному примеру **нарушения** и расскажите, как оно проявляется в эксплуатации (производительность, безопасность, надёжность).
3. Что такое **Distributed Monolith** и **Shared Database across services**? Почему наличие микросервисов в коде ещё не означает микросервисную архитектуру? Как организационная зрелость («microservices for a monolith team») влияет на успешность перехода?

---

## Билет №6

1. Сравните **Lift-and-Shift, Replatforming и Refactoring/Rearchitecting** как стратегии миграции в облако. Для каждой стратегии укажите типичный TCO/ROI-профиль, риски, требуемые изменения в команде и горизонт окупаемости.
2. Опишите **полный план миграции в облако**: Подготовка → Анализ → Стратегия → План → Исполнение → Эксплуатация → Эволюция. Какие критерии готовности (DoD) применяются на каждом этапе и почему попытка «перенести всё за выходные» обычно проваливается?
3. Объясните метрики **MTTR, MTBF, MTTD, MTTF, TTM**. Почему улучшение MTTR может быть важнее, чем улучшение MTBF, для бизнеса с пользовательским SLA? Какие архитектурные решения сокращают именно MTTR (а не MTBF)?

---

## Билет №7

1. Дайте формальное определение **распределённой системы** (P = {p₁,…,pₙ}, передача сообщений). Сформулируйте два фундаментальных **ограничения** распределённой модели и объясните, почему FLP, CAP и ABD являются их прямыми следствиями.
2. Объясните разницу между **распределённой и децентрализованной** системой. Почему децентрализация — «вынужденная мера», а распределённость — «выбор архитектора»? Приведите пример системы, которая логически централизована, но физически распределена (и наоборот).
3. Перечислите и раскройте **классические заблуждения распределённых вычислений** («сеть надёжна», «latency = 0» и т.д.). Для каждого заблуждения приведите реальный продакшен-инцидент или класс ошибок, который оно порождает.

---

## Билет №8

1. Сравните **синхронную, асинхронную и частично-синхронную** модели времени (по Dwork, Lynch, Stockmeyer). Что такое **GST (Global Stabilization Time)** и почему частично-синхронная модель — это «лучшее, что мы имеем» в реальных системах?
2. Опишите **иерархию моделей отказов**: Crash → Omission → Timing → Byzantine. Какие требования к числу узлов *n* при *f* отказов накладывает каждая модель (например, *n ≥ 3f+1* для византийских)? В каких индустриях византийская модель действительно нужна, а где это over-engineering?
3. Сформулируйте **теорему FLP (1985)**. Почему «невозможность» в FLP — это абсолютная теоретическая невозможность, а не вопрос сложности? Как современные алгоритмы (Paxos, Raft) обходят этот результат, не нарушая его?

---

## Билет №9

1. Опишите **систему кворумов** N/R/W. Докажите, что условия `R + W > N` и `W > N/2` гарантируют согласованность чтений и записей в реплицированном регистре. Какие свойства теряются, если ослабить эти условия (например, R+W=N)?
2. Сформулируйте задачу **консенсуса**: модель системы, атомарные операции Send/Receive, требования **Agreement, Validity, Termination**. Почему отсутствие атомарного broadcast и атомарной «receive+send» — это принципиальное ограничение, а не недосмотр модели?
3. Сравните **Paxos и Raft** как алгоритмы консенсуса. На каких реальных продуктах построены и тот и другой (etcd/Consul/CockroachDB vs Chubby/Spanner)? Почему авторы Raft в названии статьи написали «In Search of an **Understandable** Consensus Algorithm»?

---

## Билет №10

1. Сформулируйте теорему **CAP** (Gilbert-Lynch, 2002). Почему «выбор CA» — это миф в распределённой системе? Чем CP отличается от AP в поведении при сетевом разделении? Приведите по два примера систем категории CP и AP.
2. Сформулируйте **PACELC** (Abadi, 2012). Чем он уточняет CAP и почему в нормальном режиме (99% времени) важен компромисс **Latency ↔ Consistency**? Классифицируйте по PACELC: Spanner, Cassandra, MongoDB, Yahoo PNUTS, DynamoDB.
3. Объясните разницу между **линеаризуемостью** и **eventual consistency**. Какие промежуточные модели согласованности существуют (read-your-writes, monotonic reads, causal) и зачем они нужны?

---

## Билет №11

1. Сравните **синхронную и асинхронную коммуникацию** между сервисами. Когда оправдан переход с REST на брокер сообщений? Какие проблемы (блокировка потоков, каскадные таймауты) решает асинхронная модель, а какие — наоборот, создаёт?
2. Опишите архитектуру **Apache Kafka**: producer, topic, partition, broker, consumer, consumer group. Что такое **ключ партиционирования**, как работает алгоритм автопартиционирования (Murmur2) и почему он не гарантирует равномерного распределения? Какие практические последствия у «горячих» партиций?
3. Сравните роль **ZooKeeper и KRaft** в Kafka. Какую информацию хранит координационный слой? Почему индустрия переходит на KRaft и какие сложности возникают при миграции существующего кластера?

---

## Билет №12

1. Объясните, как настраивается **timeout** на разных уровнях стека (connection, socket/read, request, DB statement, pool acquire). Сформулируйте правило `T_caller > p99(T_callee) + slack` и объясните, почему дефолтные таймауты `HttpURLConnection` — «бомба замедленного действия».
2. Объясните анатомию **retry storm / thundering herd**. Почему ретраи без jitter синхронизируют клиентов и продлевают аварию? Сравните четыре стратегии jitter: no-jitter, full jitter, equal jitter, decorrelated jitter. Какую и когда рекомендует AWS?
3. Что такое **идемпотентность** и почему она обязательное предусловие для retry? Классифицируйте HTTP-методы (GET/PUT/DELETE/PATCH/POST) по идемпотентности. Опишите паттерн **Idempotency-Key** на примере Stripe API: как и где он хранится, какой TTL, что происходит при коллизии ключа с другим телом запроса.

---

## Билет №13

1. Опишите конечный автомат **Circuit Breaker** (CLOSED → OPEN → HALF-OPEN). Какие параметры (failure rate threshold, sliding window, wait duration, success threshold в HALF-OPEN) определяют его поведение? Почему сам по себе CB не предотвращает падение downstream, а лишь защищает upstream?
2. Объясните, почему **Retry должен быть снаружи Circuit Breaker** в композиции декораторов, а не наоборот. Что именно сломается в статистике breaker-а, если поместить его *вокруг* retry? Приведите корректный порядок: Retry → CB → TimeLimiter → fallback.
3. Сравните **Semaphore-based и Thread-pool-based Bulkhead**. Почему пул потоков на каждую downstream-зависимость дороже, но даёт защиту от RUNNABLE-блокировок? Что такое **graceful degradation** через иерархию fallback (свежие данные → stale-кэш → дефолт → быстрая ошибка)?

---

## Билет №14

1. Объясните паттерн **Saga** через последовательность локальных ACID-транзакций T₁…Tₙ с компенсациями C₁…Cₙ. Почему Saga даёт «ACD, не ACID» и что именно теряется (Isolation)? Чем компенсация *семантически отличается* от отката (например, refund ≠ undo charge)?
2. Сравните **оркестрацию и хореографию** саги. Какие компромиссы по связанности, видимости, отладке и масштабированию? На каких инструментах реализуются (Temporal, AWS Step Functions, Camunda vs Kafka/RabbitMQ + event sourcing)?
3. Почему компенсации саги должны быть **идемпотентными**? Что делать, если компенсация падает навсегда? Расскажите, как Stripe, Uber (Cadence/Temporal) и AWS Step Functions решают эти задачи на индустриальном масштабе.

---

## Билет №15

1. Объясните понятие **blast radius** и его уровни: Pod → Node → AZ → Region → Global. Как архитектурные решения (например, cell-based architecture, multi-AZ deployment) ограничивают blast radius? Свяжите концепт с Bulkhead из лекции 1.
2. Сравните **RPO и RTO** как метрики катастрофоустойчивости. Для каких типов систем (платежи, e-commerce checkout, соцсети, аналитика) приемлемы какие значения RPO/RTO? Какие архитектурные решения (sync vs async-репликация, active-active vs active-passive, backup+restore) обеспечивают эти значения?
3. Разберите кейс **AWS S3 us-east-1, 28.02.2017**: что произошло, почему пострадала сама консоль AWS, какие архитектурные уроки извлекла индустрия (multi-region S3 + CDN fallback, изоляция управляющей плоскости от данных)? Почему паттерны лекции 1 (retry, circuit breaker) не помогли пережить этот инцидент?

---

## Билет №16

1. Объясните паттерн **CQRS**. Какие конфликты требований (нормализация для записи vs денормализация для чтения, индексы для записи vs для чтения) он решает? Когда CQRS становится **over-engineering**?
2. Объясните **Event Sourcing**: `state = fold(events, initial)`. Какие плюсы (audit log, time travel, новые read-модели задним числом) и минусы (миллионы событий, эволюция схемы) он даёт? Зачем нужны **snapshots** и почему «никогда не удалять старые события» — это инвариант, а не рекомендация?
3. Опишите три стратегии **эволюции схемы событий**: versioning, upcasting, schema registry (Avro/Protobuf, forward/backward compatibility). Какие катастрофы случаются, если выкатить релиз с несовместимой схемой в систему, где лог хранится годами?

---

## Билет №17

1. Сравните **Active-Passive и Active-Active** репликацию. Почему стоимость инфраструктуры для Active-Active ~2×, но RTO ≈ 0? Какие конкретные шаги (detection, promotion, DNS update, verification) занимают типичные 5–15 минут при failover в Active-Passive?
2. Опишите четыре типичные топологии **multi-region**: read-local/write-global, multi-leader, cell-based, edge computing. Какие компромиссы накладывает cross-region latency (50–200 мс) и стоимость egress-трафика? Когда GDPR/ФЗ-152 делают multi-region не выбором, а требованием?
3. Что такое **CRDT (Conflict-free Replicated Data Types)**? На примере счётчика лайков (G-Counter) объясните, чем merge через поэлементный max отличается от Last-Write-Wins. Почему именно коммутативность и идемпотентность операций позволяют репликам сходиться без координации?

---

## Билет №18

1. Объясните философию **graceful degradation**: от бинарного «UP/DOWN» к уровням Full → Reduced → Core → Read-only → Static. Почему мониторинг «half down» так важен и какие SLA-метрики это требует пересчитывать?
2. Сравните **четыре типа feature toggles** по Hodgson: Release, Experiment, Ops, Permission. Чем **kill switch** отличается от обычного Ops toggle? Какие технические требования (< 1 сек применение, отсутствие зависимости от downstream, аудит) предъявляются к kill switch?
3. На примере **Netflix** объясните алгоритм приоритизации функций при инциденте: что отключается первым (аналитика, A/B-тесты), что — последним (просмотр видео). Почему «не должно быть ситуации, когда видео не воспроизводится из-за рекомендательной системы»?

---

## Билет №19

1. Сформулируйте **5 принципов Chaos Engineering** (steady state hypothesis, разнообразие реальных событий, эксперименты в production, автоматизация, минимизация blast radius). Почему **staging-эксперименты дают ложное чувство уверенности**?
2. Проследите **эволюцию Netflix Simian Army**: Chaos Monkey (2011) → Latency Monkey → Chaos Gorilla → Chaos Kong → ChAP (2017). От чего к чему сместился фокус: от «давайте ломать» к «давайте проверять»? Сравните с практикой **Game Days** (Google DiRT, AWS Game Days, Netflix Failure Friday).
3. Объясните принципы **blameless postmortem** (Allspaw, 2012). Почему вопрос «кто это сделал?» разрушает культуру, а «какая информация была доступна на момент действия?» — улучшает её? Какую роль играет психологическая безопасность в способности системы обучаться на инцидентах?

---

## Билет №20

1. Что такое **корпоративная архитектура (Enterprise Architecture, ISO/IEC 42010)**? Чем роль **корпоративного архитектора** отличается от **архитектора решений**? Какие пять стандартных проблем («хаотичное самоформирование», «сложность», и т.д.) она призвана решать?
2. Опишите структуру **ИТ-стратегии**: текущая ситуация, тенденции и риски, стратегические ориентиры, исследование и пилотирование технологий, информационная безопасность, инфраструктурные технологии. Почему ИТ-стратегия обязательно должна быть **итеративной** и согласованной со стратегией бизнеса?
3. Что такое **технологический суверенитет** и как он влияет на выбор облачных решений, СУБД, операционных систем и middleware в корпоративной архитектуре российской компании? Приведите примеры конкретных регуляторов (ЦБ, ФСТЭК, Минцифры, Роскомнадзор) и областей их влияния на архитектурные решения.

